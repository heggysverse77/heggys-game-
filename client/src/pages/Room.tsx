/* HeggyVerse — Room page (spec: pages/Room)
   Player list in cards, active challenge highlighted, timer bar at top.
   Canonical implementation lives in LobbyPage (lobby) + GamePage (live room). */
export { default } from './LobbyPage';
